server {
    listen 80;
    server_name synora.duckdns.org;

    # Frontend
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Backend API
    location /api/ {
        proxy_pass http://localhost:8000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Wichtig für File Uploads
        client_max_body_size 50M;
    }

    # Optional: HTTPS Redirect (wenn du später SSL einrichtest)
    # return 301 https://$server_name$request_uri;
}

# Für HTTPS mit Let's Encrypt (später):
# server {
#     listen 443 ssl;
#     server_name synora.duckdns.org;
#
#     ssl_certificate /etc/letsencrypt/live/synora.duckdns.org/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/synora.duckdns.org/privkey.pem;
#
#     location / {
#         proxy_pass http://localhost:3000;
#         # ... gleiche Proxy-Einstellungen wie oben
#     }
#
#     location /api/ {
#         proxy_pass http://localhost:8000/api/;
#         # ... gleiche Proxy-Einstellungen wie oben
#     }
# }
